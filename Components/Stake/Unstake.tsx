import React from 'react';
import { Button } from 'react-bootstrap';

const Unstake = () => {
    return (
        <>
            <Button type="submit" className="unstack-btn">
                UNSTAKE
            </Button>
        </>
    )
}

export default Unstake;